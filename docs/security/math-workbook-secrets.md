# 数学错题本密钥处理

- 当前代码、`.env.example`、测试 fixture 和普通日志不得包含真实 API key、Authorization header 或完整模型响应。
- DeepSeek live canary 仅在显式环境变量开启且服务端配置密钥时执行；普通测试永不访问真实模型。
- Git 历史中曾出现过疑似供应商密钥。仓库所有者必须在对应供应商侧吊销或轮换该密钥；本项目不会自动声称已完成轮换，也不会自动重写公共历史或 force push。
- 发现密钥时先停止传播、在供应商侧吊销/轮换，再按仓库策略处理历史清理。
